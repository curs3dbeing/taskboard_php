# Исправление ошибки "Application failed to respond"

## 🔍 Быстрая диагностика

### Шаг 1: Проверьте логи Railway

1. Откройте Railway Dashboard
2. Перейдите в ваш проект → **Deployments**
3. Выберите последний деплой
4. Нажмите **"View Logs"**

**Ищите:**
- `Fatal error`
- `Parse error` 
- `Database connection failed`
- `Class not found`
- `File not found`

### Шаг 2: Откройте диагностический скрипт

После деплоя откройте в браузере:
```
https://your-app.up.railway.app/test.php
```

Этот скрипт покажет:
- ✅ Работает ли PHP
- ✅ Подключение к базе данных
- ✅ Наличие всех файлов
- ✅ Настройки окружения

## 🔧 Наиболее частые причины

### 1. Ошибка подключения к базе данных

**Проблема:** `config.php` пытается подключиться к БД при каждом запросе, и если подключение не удается, приложение падает.

**Решение:**
- Проверьте, что MySQL сервис запущен в Railway
- Используйте **публичный хост** MySQL, а не `mysql.railway.internal`
- Публичный хост находится в настройках MySQL → "Connect" → "Public Network"

**Обновите config.php:**
```php
// Используйте публичный хост из Railway MySQL
define('DB_HOST', getenv('MYSQLHOST') ?: 'your-public-host.railway.app');
```

### 2. Отсутствие файлов

**Проблема:** Файлы не загружены в репозиторий или исключены `.gitignore`

**Решение:**
- Убедитесь, что папка `PHPMailer/` в репозитории
- Проверьте `.gitignore` - не исключает ли он нужные файлы
- Убедитесь, что все PHP файлы закоммичены

### 3. Ошибки синтаксиса PHP

**Проблема:** Синтаксические ошибки в PHP файлах

**Решение:**
- Проверьте все PHP файлы локально на ошибки
- Убедитесь, что используется PHP 8.2 (Railway по умолчанию)

### 4. Неправильная команда запуска

**Проблема:** PHP сервер не запускается или использует неправильный порт

**Решение:**
- Убедитесь, что `Procfile` содержит: `web: php -S 0.0.0.0:${PORT} -t .`
- Или используйте `nixpacks.toml` с правильной командой

### 5. База данных не инициализирована

**Проблема:** Таблицы не созданы, и приложение падает при попытке подключения

**Решение:**
- Запустите `init_db.php` после первого деплоя
- Или создайте таблицы вручную через Railway MySQL Console

## 📋 Пошаговое решение

### Вариант 1: Если проблема с базой данных

1. **Проверьте хост MySQL:**
   - В Railway откройте MySQL сервис
   - Перейдите в "Connect" → "Public Network"
   - Скопируйте публичный хост
   - Обновите `DB_HOST` в `config.php`

2. **Проверьте переменные окружения:**
   - Railway автоматически создает `MYSQLHOST`, `MYSQLDATABASE`, `MYSQLUSER`, `MYSQLPASSWORD`
   - Убедитесь, что `config.php` использует их

3. **Инициализируйте базу данных:**
   ```
   https://your-app.up.railway.app/init_db.php?init=railway-init-2024
   ```

### Вариант 2: Если проблема с запуском

1. **Проверьте Procfile:**
   ```bash
   web: php -S 0.0.0.0:${PORT} -t .
   ```

2. **Проверьте nixpacks.toml:**
   ```toml
   [start]
   cmd = "php -S 0.0.0.0:${PORT} -t ."
   ```

3. **Пересоберите проект:**
   - В Railway нажмите "Redeploy"
   - Или сделайте новый коммит в GitHub

### Вариант 3: Если проблема с файлами

1. **Проверьте наличие PHPMailer:**
   ```bash
   # Локально проверьте, что папка PHPMailer/ есть
   ls -la PHPMailer/
   ```

2. **Убедитесь, что файлы в репозитории:**
   ```bash
   git add .
   git commit -m "Add all files"
   git push
   ```

3. **Проверьте .gitignore:**
   - Убедитесь, что он не исключает нужные файлы

## 🚀 Что я уже исправил

1. ✅ Улучшена обработка ошибок в `config.php` - не падает при ошибке БД
2. ✅ Добавлена обработка ошибок в `index.php`
3. ✅ Создан `test.php` для диагностики
4. ✅ Создан `health.php` для health checks
5. ✅ Обновлен `nixpacks.toml` с правильной командой запуска
6. ✅ Обновлен `Procfile` с правильным синтаксисом

## 📝 Следующие шаги

1. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Fix Railway deployment issues"
   git push
   ```

2. **Дождитесь деплоя** в Railway

3. **Откройте test.php:**
   ```
   https://your-app.up.railway.app/test.php
   ```

4. **Проверьте логи** если все еще не работает

5. **Если база данных не инициализирована:**
   ```
   https://your-app.up.railway.app/init_db.php?init=railway-init-2024
   ```

## 💡 Важно

**Самое главное** - проверьте логи Railway! Там будет точная причина ошибки.

Если после всех исправлений проблема остается, пришлите:
1. Логи из Railway (последние 50 строк)
2. Результат `test.php`
3. Скриншот ошибки

